
import { dollarRating } from 'restoran/helpers/dollar-rating';
import { module, test } from 'qunit';

module('Unit | Helper | dollar rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dollarRating([42]);
  assert.ok(result);
});

