
import { isWithinRange } from 'restoran/helpers/is-within-range';
import { module, test } from 'qunit';

module('Unit | Helper | is within range');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isWithinRange([42]);
  assert.ok(result);
});

