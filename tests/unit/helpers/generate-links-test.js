
import { generateLinks } from 'restoran/helpers/generate-links';
import { module, test } from 'qunit';

module('Unit | Helper | generate links');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = generateLinks([42]);
  assert.ok(result);
});

